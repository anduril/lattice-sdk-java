/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.anduril.resources.tasks.types;

import com.anduril.types.ManualControlFrameEvent;
import com.anduril.types.StreamHeartbeat;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class StreamManualControlFramesResponse {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private StreamManualControlFramesResponse(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static StreamManualControlFramesResponse heartbeat(StreamHeartbeat value) {
        return new StreamManualControlFramesResponse(new HeartbeatValue(value));
    }

    public static StreamManualControlFramesResponse manualControlFrame(ManualControlFrameEvent value) {
        return new StreamManualControlFramesResponse(new ManualControlFrameValue(value));
    }

    public boolean isHeartbeat() {
        return value instanceof HeartbeatValue;
    }

    public boolean isManualControlFrame() {
        return value instanceof ManualControlFrameValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<StreamHeartbeat> getHeartbeat() {
        if (isHeartbeat()) {
            return Optional.of(((HeartbeatValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ManualControlFrameEvent> getManualControlFrame() {
        if (isManualControlFrame()) {
            return Optional.of(((ManualControlFrameValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof StreamManualControlFramesResponse
                && value.equals(((StreamManualControlFramesResponse) other).value);
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public String toString() {
        return value.toString();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitHeartbeat(StreamHeartbeat heartbeat);

        T visitManualControlFrame(ManualControlFrameEvent manualControlFrame);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "event", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(HeartbeatValue.class), @JsonSubTypes.Type(ManualControlFrameValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("heartbeat")
    @JsonIgnoreProperties("event")
    private static final class HeartbeatValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "event", allowSetters = true)
        private StreamHeartbeat value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private HeartbeatValue() {}

        private HeartbeatValue(StreamHeartbeat value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitHeartbeat(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof HeartbeatValue && equalTo((HeartbeatValue) other);
        }

        private boolean equalTo(HeartbeatValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamManualControlFramesResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("manual_control_frame")
    @JsonIgnoreProperties("event")
    private static final class ManualControlFrameValue implements Value {
        @JsonUnwrapped
        @JsonIgnoreProperties(value = "event", allowSetters = true)
        private ManualControlFrameEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ManualControlFrameValue() {}

        private ManualControlFrameValue(ManualControlFrameEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitManualControlFrame(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ManualControlFrameValue && equalTo((ManualControlFrameValue) other);
        }

        private boolean equalTo(ManualControlFrameValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamManualControlFramesResponse{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("event")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamManualControlFramesResponse{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
