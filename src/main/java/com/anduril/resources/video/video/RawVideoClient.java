/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.anduril.resources.video.video;

import com.anduril.core.ClientOptions;
import com.anduril.core.LatticeApiException;
import com.anduril.core.LatticeException;
import com.anduril.core.LatticeHttpResponse;
import com.anduril.core.MediaTypes;
import com.anduril.core.ObjectMappers;
import com.anduril.core.QueryStringMapper;
import com.anduril.core.RequestOptions;
import com.anduril.core.RetryInterceptor;
import com.anduril.resources.video.errors.BadRequestError;
import com.anduril.resources.video.errors.ConflictError;
import com.anduril.resources.video.errors.ForbiddenError;
import com.anduril.resources.video.errors.InternalServerError;
import com.anduril.resources.video.errors.NotFoundError;
import com.anduril.resources.video.errors.ServiceUnavailableError;
import com.anduril.resources.video.errors.TooManyRequestsError;
import com.anduril.resources.video.errors.UnauthorizedError;
import com.anduril.resources.video.types.CreateEgressStreamResponse;
import com.anduril.resources.video.types.CreateIngressStreamResponse;
import com.anduril.resources.video.types.DeleteEgressStreamResponse;
import com.anduril.resources.video.types.DeleteIngressStreamResponse;
import com.anduril.resources.video.types.GetEgressStreamResponse;
import com.anduril.resources.video.types.GetIngressStreamResponse;
import com.anduril.resources.video.types.GoogleRpcStatus;
import com.anduril.resources.video.types.ListEgressStreamsResponse;
import com.anduril.resources.video.types.ListIngressStreamsResponse;
import com.anduril.resources.video.video.requests.CreateEgressStreamRequest;
import com.anduril.resources.video.video.requests.CreateIngressStreamRequest;
import com.anduril.resources.video.video.requests.DeleteEgressStreamRequest;
import com.anduril.resources.video.video.requests.DeleteIngressStreamRequest;
import com.anduril.resources.video.video.requests.GetEgressStreamRequest;
import com.anduril.resources.video.video.requests.GetIngressStreamRequest;
import com.anduril.resources.video.video.requests.ListEgressStreamsRequest;
import com.anduril.resources.video.video.requests.ListIngressStreamsRequest;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawVideoClient {
    protected final ClientOptions clientOptions;

    public RawVideoClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Returns a list of active egress stream objects.
     * Results are ordered by egress stream create time. If the
     * egress backend is unreachable, the listed streams might be stale or degraded.
     */
    public LatticeHttpResponse<ListEgressStreamsResponse> listEgressStreams() {
        return listEgressStreams(ListEgressStreamsRequest.builder().build());
    }

    /**
     * Returns a list of active egress stream objects.
     * Results are ordered by egress stream create time. If the
     * egress backend is unreachable, the listed streams might be stale or degraded.
     */
    public LatticeHttpResponse<ListEgressStreamsResponse> listEgressStreams(RequestOptions requestOptions) {
        return listEgressStreams(ListEgressStreamsRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a list of active egress stream objects.
     * Results are ordered by egress stream create time. If the
     * egress backend is unreachable, the listed streams might be stale or degraded.
     */
    public LatticeHttpResponse<ListEgressStreamsResponse> listEgressStreams(ListEgressStreamsRequest request) {
        return listEgressStreams(request, null);
    }

    /**
     * Returns a list of active egress stream objects.
     * Results are ordered by egress stream create time. If the
     * egress backend is unreachable, the listed streams might be stale or degraded.
     */
    public LatticeHttpResponse<ListEgressStreamsResponse> listEgressStreams(
            ListEgressStreamsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/v1/video/egress_streams");
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "pageSize", request.getPageSize().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "pageToken", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new LatticeHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListEgressStreamsResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new LatticeApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new LatticeException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates an egress stream that publishes a live stream to a downstream consumer.
     * A stream in <code>STREAM_STATUS_UNAVAILABLE</code> is rejected as not-live.
     */
    public LatticeHttpResponse<CreateEgressStreamResponse> createEgressStream() {
        return createEgressStream(CreateEgressStreamRequest.builder().build());
    }

    /**
     * Creates an egress stream that publishes a live stream to a downstream consumer.
     * A stream in <code>STREAM_STATUS_UNAVAILABLE</code> is rejected as not-live.
     */
    public LatticeHttpResponse<CreateEgressStreamResponse> createEgressStream(RequestOptions requestOptions) {
        return createEgressStream(CreateEgressStreamRequest.builder().build(), requestOptions);
    }

    /**
     * Creates an egress stream that publishes a live stream to a downstream consumer.
     * A stream in <code>STREAM_STATUS_UNAVAILABLE</code> is rejected as not-live.
     */
    public LatticeHttpResponse<CreateEgressStreamResponse> createEgressStream(CreateEgressStreamRequest request) {
        return createEgressStream(request, null);
    }

    /**
     * Creates an egress stream that publishes a live stream to a downstream consumer.
     * A stream in <code>STREAM_STATUS_UNAVAILABLE</code> is rejected as not-live.
     */
    public LatticeHttpResponse<CreateEgressStreamResponse> createEgressStream(
            CreateEgressStreamRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/v1/video/egress_streams");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new LatticeHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateEgressStreamResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new LatticeApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new LatticeException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves an egress stream object and its associated metadata.
     */
    public LatticeHttpResponse<GetEgressStreamResponse> getEgressStream(String egressId) {
        return getEgressStream(egressId, GetEgressStreamRequest.builder().build());
    }

    /**
     * Retrieves an egress stream object and its associated metadata.
     */
    public LatticeHttpResponse<GetEgressStreamResponse> getEgressStream(
            String egressId, RequestOptions requestOptions) {
        return getEgressStream(egressId, GetEgressStreamRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves an egress stream object and its associated metadata.
     */
    public LatticeHttpResponse<GetEgressStreamResponse> getEgressStream(
            String egressId, GetEgressStreamRequest request) {
        return getEgressStream(egressId, request, null);
    }

    /**
     * Retrieves an egress stream object and its associated metadata.
     */
    public LatticeHttpResponse<GetEgressStreamResponse> getEgressStream(
            String egressId, GetEgressStreamRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/v1/video/egress_streams")
                .addPathSegment(egressId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new LatticeHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetEgressStreamResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new LatticeApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new LatticeException("Network error executing HTTP request", e);
        }
    }

    /**
     * Deletes the egress stream for a live stream. Returns <code>NOT_FOUND</code> if no matching active
     * egress stream exists.
     */
    public LatticeHttpResponse<DeleteEgressStreamResponse> deleteEgressStream(String egressId) {
        return deleteEgressStream(egressId, DeleteEgressStreamRequest.builder().build());
    }

    /**
     * Deletes the egress stream for a live stream. Returns <code>NOT_FOUND</code> if no matching active
     * egress stream exists.
     */
    public LatticeHttpResponse<DeleteEgressStreamResponse> deleteEgressStream(
            String egressId, RequestOptions requestOptions) {
        return deleteEgressStream(egressId, DeleteEgressStreamRequest.builder().build(), requestOptions);
    }

    /**
     * Deletes the egress stream for a live stream. Returns <code>NOT_FOUND</code> if no matching active
     * egress stream exists.
     */
    public LatticeHttpResponse<DeleteEgressStreamResponse> deleteEgressStream(
            String egressId, DeleteEgressStreamRequest request) {
        return deleteEgressStream(egressId, request, null);
    }

    /**
     * Deletes the egress stream for a live stream. Returns <code>NOT_FOUND</code> if no matching active
     * egress stream exists.
     */
    public LatticeHttpResponse<DeleteEgressStreamResponse> deleteEgressStream(
            String egressId, DeleteEgressStreamRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/v1/video/egress_streams")
                .addPathSegment(egressId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new LatticeHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteEgressStreamResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new LatticeApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new LatticeException("Network error executing HTTP request", e);
        }
    }

    /**
     * Returns a list of top level ingress stream objects, including ingress streams and internal
     * Anduril streams. Will only return active streams.
     * Results are ordered by ingress stream create time.
     */
    public LatticeHttpResponse<ListIngressStreamsResponse> listIngressStreams() {
        return listIngressStreams(ListIngressStreamsRequest.builder().build());
    }

    /**
     * Returns a list of top level ingress stream objects, including ingress streams and internal
     * Anduril streams. Will only return active streams.
     * Results are ordered by ingress stream create time.
     */
    public LatticeHttpResponse<ListIngressStreamsResponse> listIngressStreams(RequestOptions requestOptions) {
        return listIngressStreams(ListIngressStreamsRequest.builder().build(), requestOptions);
    }

    /**
     * Returns a list of top level ingress stream objects, including ingress streams and internal
     * Anduril streams. Will only return active streams.
     * Results are ordered by ingress stream create time.
     */
    public LatticeHttpResponse<ListIngressStreamsResponse> listIngressStreams(ListIngressStreamsRequest request) {
        return listIngressStreams(request, null);
    }

    /**
     * Returns a list of top level ingress stream objects, including ingress streams and internal
     * Anduril streams. Will only return active streams.
     * Results are ordered by ingress stream create time.
     */
    public LatticeHttpResponse<ListIngressStreamsResponse> listIngressStreams(
            ListIngressStreamsRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/v1/video/ingress_streams");
        if (request.getPageSize().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "pageSize", request.getPageSize().get(), false);
        }
        if (request.getPageToken().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "pageToken", request.getPageToken().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new LatticeHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListIngressStreamsResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 500:
                        throw new InternalServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new LatticeApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new LatticeException("Network error executing HTTP request", e);
        }
    }

    /**
     * Creates a video ingress stream, returning metadata that you can use to stream live video to
     * Lattice. Exactly one of <code>rtsp</code> or <code>srt</code> must be set on the request.
     */
    public LatticeHttpResponse<CreateIngressStreamResponse> createIngressStream() {
        return createIngressStream(CreateIngressStreamRequest.builder().build());
    }

    /**
     * Creates a video ingress stream, returning metadata that you can use to stream live video to
     * Lattice. Exactly one of <code>rtsp</code> or <code>srt</code> must be set on the request.
     */
    public LatticeHttpResponse<CreateIngressStreamResponse> createIngressStream(RequestOptions requestOptions) {
        return createIngressStream(CreateIngressStreamRequest.builder().build(), requestOptions);
    }

    /**
     * Creates a video ingress stream, returning metadata that you can use to stream live video to
     * Lattice. Exactly one of <code>rtsp</code> or <code>srt</code> must be set on the request.
     */
    public LatticeHttpResponse<CreateIngressStreamResponse> createIngressStream(CreateIngressStreamRequest request) {
        return createIngressStream(request, null);
    }

    /**
     * Creates a video ingress stream, returning metadata that you can use to stream live video to
     * Lattice. Exactly one of <code>rtsp</code> or <code>srt</code> must be set on the request.
     */
    public LatticeHttpResponse<CreateIngressStreamResponse> createIngressStream(
            CreateIngressStreamRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/v1/video/ingress_streams");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new LatticeHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateIngressStreamResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 409:
                        throw new ConflictError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new LatticeApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new LatticeException("Network error executing HTTP request", e);
        }
    }

    /**
     * Retrieves a top level ingress stream object and its associated metadata. This includes
     * ingress streams and internal Anduril streams.
     */
    public LatticeHttpResponse<GetIngressStreamResponse> getIngressStream(String ingressId) {
        return getIngressStream(ingressId, GetIngressStreamRequest.builder().build());
    }

    /**
     * Retrieves a top level ingress stream object and its associated metadata. This includes
     * ingress streams and internal Anduril streams.
     */
    public LatticeHttpResponse<GetIngressStreamResponse> getIngressStream(
            String ingressId, RequestOptions requestOptions) {
        return getIngressStream(ingressId, GetIngressStreamRequest.builder().build(), requestOptions);
    }

    /**
     * Retrieves a top level ingress stream object and its associated metadata. This includes
     * ingress streams and internal Anduril streams.
     */
    public LatticeHttpResponse<GetIngressStreamResponse> getIngressStream(
            String ingressId, GetIngressStreamRequest request) {
        return getIngressStream(ingressId, request, null);
    }

    /**
     * Retrieves a top level ingress stream object and its associated metadata. This includes
     * ingress streams and internal Anduril streams.
     */
    public LatticeHttpResponse<GetIngressStreamResponse> getIngressStream(
            String ingressId, GetIngressStreamRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/v1/video/ingress_streams")
                .addPathSegment(ingressId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new LatticeHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetIngressStreamResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new LatticeApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new LatticeException("Network error executing HTTP request", e);
        }
    }

    /**
     * Deletes a video ingress stream and transitions the stream to <code>STREAM_STATUS_ARCHIVED</code>.
     * Any egress streams consuming this stream will be stopped automatically.
     */
    public LatticeHttpResponse<DeleteIngressStreamResponse> deleteIngressStream(String ingressId) {
        return deleteIngressStream(
                ingressId, DeleteIngressStreamRequest.builder().build());
    }

    /**
     * Deletes a video ingress stream and transitions the stream to <code>STREAM_STATUS_ARCHIVED</code>.
     * Any egress streams consuming this stream will be stopped automatically.
     */
    public LatticeHttpResponse<DeleteIngressStreamResponse> deleteIngressStream(
            String ingressId, RequestOptions requestOptions) {
        return deleteIngressStream(
                ingressId, DeleteIngressStreamRequest.builder().build(), requestOptions);
    }

    /**
     * Deletes a video ingress stream and transitions the stream to <code>STREAM_STATUS_ARCHIVED</code>.
     * Any egress streams consuming this stream will be stopped automatically.
     */
    public LatticeHttpResponse<DeleteIngressStreamResponse> deleteIngressStream(
            String ingressId, DeleteIngressStreamRequest request) {
        return deleteIngressStream(ingressId, request, null);
    }

    /**
     * Deletes a video ingress stream and transitions the stream to <code>STREAM_STATUS_ARCHIVED</code>.
     * Any egress streams consuming this stream will be stopped automatically.
     */
    public LatticeHttpResponse<DeleteIngressStreamResponse> deleteIngressStream(
            String ingressId, DeleteIngressStreamRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("api/v1/video/ingress_streams")
                .addPathSegment(ingressId);
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new LatticeHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DeleteIngressStreamResponse.class),
                        response);
            }
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 401:
                        throw new UnauthorizedError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 403:
                        throw new ForbiddenError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                    case 503:
                        throw new ServiceUnavailableError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GoogleRpcStatus.class),
                                response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new LatticeApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new LatticeException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new LatticeException("Network error executing HTTP request", e);
        }
    }
}
