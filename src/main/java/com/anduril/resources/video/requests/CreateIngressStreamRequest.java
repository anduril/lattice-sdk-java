/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.anduril.resources.video.requests;

import com.anduril.core.ObjectMappers;
import com.anduril.resources.video.types.RtspSettings;
import com.anduril.resources.video.types.SrtSettings;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CreateIngressStreamRequest.Builder.class)
public final class CreateIngressStreamRequest {
    private final Optional<String> ingressId;

    private final Optional<String> title;

    private final Optional<Map<String, Object>> mpegTs;

    private final Optional<RtspSettings> rtsp;

    private final Optional<SrtSettings> srt;

    private final Map<String, Object> additionalProperties;

    private CreateIngressStreamRequest(
            Optional<String> ingressId,
            Optional<String> title,
            Optional<Map<String, Object>> mpegTs,
            Optional<RtspSettings> rtsp,
            Optional<SrtSettings> srt,
            Map<String, Object> additionalProperties) {
        this.ingressId = ingressId;
        this.title = title;
        this.mpegTs = mpegTs;
        this.rtsp = rtsp;
        this.srt = srt;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Caller-supplied identifier for the new stream. If omitted, the service generates a GUID.
     * If supplied, a consistent and recognizable pattern is recommended. A common convention
     * is a group prefix (organization, platform, or asset) followed by a specific identifier
     * using underscore or dot as a separator, for example, <code>drone_1</code>, <code>vessel_2</code>, or
     * <code>teamalpha.drone1</code>.
     * <p>When supplied, an ingress_id must be between 4 and 36 characters long and use only
     * printable ASCII characters with no spaces; the 36-character ceiling leaves room for a
     * full GUID. A value outside that length range, or one containing spaces, control
     * characters, or non-ASCII characters, is rejected, as is an ingress_id that another
     * ingress stream is already using.</p>
     */
    @JsonProperty("ingressId")
    public Optional<String> getIngressId() {
        return ingressId;
    }

    /**
     * @return Human-readable title for the stream. A title is required: surrounding whitespace is
     * trimmed before it is stored, and what remains must be non-empty, valid UTF-8, and no
     * longer than 64 characters. Otherwise the request is rejected.
     */
    @JsonProperty("title")
    public Optional<String> getTitle() {
        return title;
    }

    /**
     * @return Receive an MPEG-TS push from the producer. The service allocates a UDP port and
     * returns the URL the producer must push to in CreateIngressStreamResponse.
     * <p>MPEG-TS ingress is supported only at the edge, in closed networks. When Lattice
     * runs in a cloud environment reached over the public internet, MPEG-TS ingress may
     * be disabled per deployment. When it is disabled, a request that selects mpeg_ts is
     * rejected with a gRPC error rather than accepted, so callers should be prepared to
     * fall back to RTSP or SRT. An MPEG-TS stream created at the edge can still be listed
     * and inspected on the IngressStream read model even when cloud ingress is disabled.</p>
     */
    @JsonProperty("mpegTs")
    public Optional<Map<String, Object>> getMpegTs() {
        return mpegTs;
    }

    /**
     * @return Pull from a caller-supplied RTSP URL.
     */
    @JsonProperty("rtsp")
    public Optional<RtspSettings> getRtsp() {
        return rtsp;
    }

    /**
     * @return Receive an SRT push from the producer. The service returns a URL and session_id
     * in CreateIngressStreamResponse.
     */
    @JsonProperty("srt")
    public Optional<SrtSettings> getSrt() {
        return srt;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CreateIngressStreamRequest && equalTo((CreateIngressStreamRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CreateIngressStreamRequest other) {
        return ingressId.equals(other.ingressId)
                && title.equals(other.title)
                && mpegTs.equals(other.mpegTs)
                && rtsp.equals(other.rtsp)
                && srt.equals(other.srt);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.ingressId, this.title, this.mpegTs, this.rtsp, this.srt);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<String> ingressId = Optional.empty();

        private Optional<String> title = Optional.empty();

        private Optional<Map<String, Object>> mpegTs = Optional.empty();

        private Optional<RtspSettings> rtsp = Optional.empty();

        private Optional<SrtSettings> srt = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(CreateIngressStreamRequest other) {
            ingressId(other.getIngressId());
            title(other.getTitle());
            mpegTs(other.getMpegTs());
            rtsp(other.getRtsp());
            srt(other.getSrt());
            return this;
        }

        /**
         * <p>Caller-supplied identifier for the new stream. If omitted, the service generates a GUID.
         * If supplied, a consistent and recognizable pattern is recommended. A common convention
         * is a group prefix (organization, platform, or asset) followed by a specific identifier
         * using underscore or dot as a separator, for example, <code>drone_1</code>, <code>vessel_2</code>, or
         * <code>teamalpha.drone1</code>.</p>
         * <p>When supplied, an ingress_id must be between 4 and 36 characters long and use only
         * printable ASCII characters with no spaces; the 36-character ceiling leaves room for a
         * full GUID. A value outside that length range, or one containing spaces, control
         * characters, or non-ASCII characters, is rejected, as is an ingress_id that another
         * ingress stream is already using.</p>
         */
        @JsonSetter(value = "ingressId", nulls = Nulls.SKIP)
        public Builder ingressId(Optional<String> ingressId) {
            this.ingressId = ingressId;
            return this;
        }

        public Builder ingressId(String ingressId) {
            this.ingressId = Optional.ofNullable(ingressId);
            return this;
        }

        /**
         * <p>Human-readable title for the stream. A title is required: surrounding whitespace is
         * trimmed before it is stored, and what remains must be non-empty, valid UTF-8, and no
         * longer than 64 characters. Otherwise the request is rejected.</p>
         */
        @JsonSetter(value = "title", nulls = Nulls.SKIP)
        public Builder title(Optional<String> title) {
            this.title = title;
            return this;
        }

        public Builder title(String title) {
            this.title = Optional.ofNullable(title);
            return this;
        }

        /**
         * <p>Receive an MPEG-TS push from the producer. The service allocates a UDP port and
         * returns the URL the producer must push to in CreateIngressStreamResponse.</p>
         * <p>MPEG-TS ingress is supported only at the edge, in closed networks. When Lattice
         * runs in a cloud environment reached over the public internet, MPEG-TS ingress may
         * be disabled per deployment. When it is disabled, a request that selects mpeg_ts is
         * rejected with a gRPC error rather than accepted, so callers should be prepared to
         * fall back to RTSP or SRT. An MPEG-TS stream created at the edge can still be listed
         * and inspected on the IngressStream read model even when cloud ingress is disabled.</p>
         */
        @JsonSetter(value = "mpegTs", nulls = Nulls.SKIP)
        public Builder mpegTs(Optional<Map<String, Object>> mpegTs) {
            this.mpegTs = mpegTs;
            return this;
        }

        public Builder mpegTs(Map<String, Object> mpegTs) {
            this.mpegTs = Optional.ofNullable(mpegTs);
            return this;
        }

        /**
         * <p>Pull from a caller-supplied RTSP URL.</p>
         */
        @JsonSetter(value = "rtsp", nulls = Nulls.SKIP)
        public Builder rtsp(Optional<RtspSettings> rtsp) {
            this.rtsp = rtsp;
            return this;
        }

        public Builder rtsp(RtspSettings rtsp) {
            this.rtsp = Optional.ofNullable(rtsp);
            return this;
        }

        /**
         * <p>Receive an SRT push from the producer. The service returns a URL and session_id
         * in CreateIngressStreamResponse.</p>
         */
        @JsonSetter(value = "srt", nulls = Nulls.SKIP)
        public Builder srt(Optional<SrtSettings> srt) {
            this.srt = srt;
            return this;
        }

        public Builder srt(SrtSettings srt) {
            this.srt = Optional.ofNullable(srt);
            return this;
        }

        public CreateIngressStreamRequest build() {
            return new CreateIngressStreamRequest(ingressId, title, mpegTs, rtsp, srt, additionalProperties);
        }

        public Builder additionalProperty(String key, Object value) {
            this.additionalProperties.put(key, value);
            return this;
        }

        public Builder additionalProperties(Map<String, Object> additionalProperties) {
            this.additionalProperties.putAll(additionalProperties);
            return this;
        }
    }
}
